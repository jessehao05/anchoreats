# Copy to backend/.env (gitignored, local only).

MONGO_URI=
ANALYTICS_USERNAME=
ANALYTICS_PASSWORD=
ANALYTICS_SECRET=

# Leave unset — the code defaults are correct in dev.
# FRONTEND_URL=http://localhost:5173   # only if Vite isn't on 5173
# PORT=5001
# NODE_ENV — omit, so Express runs in development mode
