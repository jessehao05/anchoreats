# Reference only — set these in the Render dashboard. backend/.env never reaches Render.

MONGO_URI=
ANALYTICS_USERNAME=
ANALYTICS_PASSWORD=
ANALYTICS_SECRET=
FRONTEND_URL=https://anchoreats.vercel.app
NODE_ENV=production

# Do NOT set PORT — Render injects it.
