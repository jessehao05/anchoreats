# Reference only — set this in the Vercel dashboard. frontend/.env never reaches Vercel.

VITE_API_URL=https://your-app.onrender.com/api

# Inlined at build time — changing it in Vercel requires a redeploy.
# VITE_ variables are public (in the client bundle), so no secrets here.
